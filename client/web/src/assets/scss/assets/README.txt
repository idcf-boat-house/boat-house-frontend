All files in this directory are external assets which AppStrap makes use of. 

The assets used are:

1. bootstrap_src
This is the Bootstrap 4.x Sass source files. AppStrap uses the _variables & mixins files only.