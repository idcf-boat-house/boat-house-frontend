spring:
  application:
    #应用名称
    name: springboot-demo
  #jdbc 配置文件内容
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/BoatHouse?characterEncoding=utf-8
    schema: classpath:db/schema.sql
    data: classpath:db/data.sql
    username: root
    password: 0000

server:
  #访问端口号
  port: 8080
  tomcat:
    #编码格式
    uri-encoding: utf-8
  servlet:
    #Api路由前缀
    context-path: /api/v1.0

## 本地输出日志详情,本地需要更改日志文件中的路径配置为个人电脑下的路径
logging:
  config: classpath:log4j2.xml
  file:
    path: logs/